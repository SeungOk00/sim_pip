--find-links https://data.dgl.ai/wheels/torch-2.3/cu121/repo.html

# Core dependencies
pyyaml>=6.0
dataclasses-json>=0.6.0
omegaconf
hydra-core 
pyrsistent

# Deep learning frameworks
dgl
torchdata<0.8.0
e3nn
torch>=2.3.1,<2.7

# Structure prediction tools
# chai_lab==0.6.1  # Installed separately in step 6
# boltz  # Installed separately in step 7

# PyRosetta (installed separately via pyrosetta-installer)
pyrosetta-installer

# DockQ for structure quality assessment
# DockQ  # Requires compilation, install separately if needed

# Scientific computing
numpy>=1.21.0  # Will use pre-built wheel
# scipy>=1.7.0  # Installed separately via conda to avoid Fortran compilation issues
pandas>=1.3.0

# Bioinformatics
biopython>=1.79

# Optimization (for NSGA-II in Phase 4)
# pymoo>=0.6.0  # Installed separately via conda (requires scipy)

# Utilities
tqdm>=4.62.0
requests>=2.26.0
